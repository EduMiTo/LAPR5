export interface IAnonymizedUser {
    userDTO: {
      domainId: string;
      firstName: string;
      lastName: string;
      email: string;
      password: string;
      role: string;
      phone: string;
    }
  }